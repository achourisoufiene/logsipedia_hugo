# Logsipedia - Log-Driven pSEO Platform

This is a Hugo-based site that automatically generates error solutions from GitHub issues.

## Deployment

1. Push this repository to GitHub.
2. Connect to Cloudflare Pages (or Netlify) with:
   - Build command: `hugo`
   - Output directory: `public`
3. Set environment variables if needed.